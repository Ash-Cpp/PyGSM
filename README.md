# Python学生成绩管理系统(By Ash)
# tabulate库
能解决CLI界面下的中文对齐问题
`pip install tabulate`
# config.json
## defaultMode
程序界面的默认选项,有三种模式
`cli:命令行界面`
`gui:图形化界面`
`auto:跟随上次结束时的界面`
## lastMode
上一次程序正常结束时的界面，有两种模式
`cli:命令行界面`
`gui:图形化界面`